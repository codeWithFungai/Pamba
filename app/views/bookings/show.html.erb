<div class=" booking_box text-center rounded-4">
  <div class="booking_text">
    <h1 class="card--title">BOOKING CONFIRMED &nbsp; &nbsp;<i class="fa-solid fa-calendar-days"></i></h1>

    <h3 style="color: white;">Location: <%= @listing.address %>, <%= @listing.city %></h3>
    <h3 class="card--title">Check In: <strong><%= @booking.start_date %></strong>  Check Out: <strong><%= @booking.end_date %> <i class="fa-regular fa-square-check"></i></strong></h3>

    <p style="color: white;">Total Cost Of Your Stay: €<%= (@listing.price * (@booking.end_date - @booking.start_date)).to_int %></p>
  </div>
</div>

<div class="text-center mb-5">
   <h1><%= link_to bookings_path, class: "btn btn-primary btn-flat rounded-5" do %>
      <p>Go to the dashboard</p>
  <% end %></h1>
</div>
